class ShelterImage {
    id?: number;
    imageUrl ?: string;

    constructor(
        id?: number,
    imageUrl ?: string,
    ) {
        this.id = id;
        this.imageUrl = imageUrl;
    }
}
export default ShelterImage