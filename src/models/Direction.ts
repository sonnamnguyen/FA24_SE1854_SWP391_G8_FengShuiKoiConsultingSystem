class Direction {
    id?: number;
    direction?: string;
    

    constructor(
        id?: number,
        direction?: string,
        
    ) {
        this.id = id;
        this.direction = direction; 
    }
}

export default Direction;
